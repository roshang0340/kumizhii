{
  "name": "daily-journal-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-auth-library": "^9.15.1",
    "multer": "^1.4.5-lts.1"
  }
}
